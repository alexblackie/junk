---
spring:
  mustache:
    suffix: .html
  cloud:
    azure:
      storage:
        blob:
          account-name: exampleaccountname
          account-key: "xxx"
          endpoint: "https://${spring.cloud.azure.storage.blob.account-name}.blob.core.windows.net"

junk:
  picDataService: local
  picDir: pics
